{
  "name": "automation-website-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && python main.py\"",
    "build": "cd frontend && npm install && npm run build",
    "start": "concurrently \"cd backend && . venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000\" \"cd frontend && npm run start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
