[phases.setup]
nixPkgs = ["nodejs", "python311", "gcc"]

[phases.install]
cmds = ["npm install", "cd backend && python -m venv venv && . venv/bin/activate && pip install -r requirements.txt"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
